import { gunzipSync } from "node:zlib";
import { NextResponse } from "next/server";
import type { SourceStatus, StandbyAdvice, WeatherSnapshot } from "@/lib/types";
import type { MeasurementSite, RoadMetringPoint } from "@/lib/ndw";
import { mapMeasurementSites, matrixSummary, parseEvents, parseMatrix, parseRawMeasurementSites, parseRoadMetringPoints, parseTrafficSamples, stripTags, tagValue } from "@/lib/ndw";
import { OPERATOR_LOCATIONS, buildSegments, findStandby, parseRwsRestAreas, segmentEvents, segmentMatrix, sensorMetrics, weatherScore, type CandidateLocation } from "@/lib/engine";

export const dynamic="force-dynamic";export const runtime="nodejs";
const URLS={current:"https://opendata.ndw.nu/actueel_beeld.xml.gz",matrix:"https://opendata.ndw.nu/Matrixsignaalinformatie.xml.gz",traffic:"https://opendata.ndw.nu/trafficspeed.xml.gz",sites:"https://opendata.ndw.nu/measurement_current.xml.gz",rwsAreas:"https://geo.rijkswaterstaat.nl/services/ogc/gdr/omgevingswet/ows?service=WFS&version=2.0.0&request=GetFeature&typeName=wegen_verzorgingsplaatsen_beperkingengebied_rijk&outputFormat=json&content-disposition=attachment&srsName=EPSG%3A4326",rwsMetering:"https://geo.rijkswaterstaat.nl/arcgis/rest/services/GDR/nwb_metrering/MapServer/2/query",weather:"https://api.open-meteo.com/v1/forecast"};
const STATIC_TTL=6*60*60*1000;
type StaticContext={expires:number;sites:MeasurementSite[];roadPoints:RoadMetringPoint[];rwsAreas:CandidateLocation[];rawSiteCount:number;siteUpdatedAt:string|null;meteringUpdatedAt:string|null;areasUpdatedAt:string|null};
let staticCache:StaticContext|null=null;

const fetchText=async(url:string,revalidate:number)=>{const r=await fetch(url,{next:{revalidate},headers:{"user-agent":"StandbyRadar/0.4"}});if(!r.ok)throw new Error(`HTTP ${r.status}`);const b=Buffer.from(await r.arrayBuffer()),text=b[0]===31&&b[1]===139?gunzipSync(b).toString("utf8"):b.toString("utf8");return{text,updatedAt:r.headers.get("last-modified")??r.headers.get("date")};};
async function fetchRoadPoints(){const all:RoadMetringPoint[]=[];let offset=0,updatedAt:string|null=null;for(let page=0;page<30;page++){const p=new URLSearchParams({where:"a_n_nr LIKE 'A%'",outFields:"a_n_nr,l_r,hectometer,hectomtrng",returnGeometry:"true",geometry:"4.20,51.15,6.55,52.30",geometryType:"esriGeometryEnvelope",inSR:"4326",spatialRel:"esriSpatialRelIntersects",outSR:"4326",resultOffset:String(offset),resultRecordCount:"2000",f:"geojson"}),r=await fetch(`${URLS.rwsMetering}?${p}`,{next:{revalidate:21600},headers:{"user-agent":"StandbyRadar/0.4"}});if(!r.ok)throw new Error(`RWS metrering HTTP ${r.status}`);updatedAt=r.headers.get("last-modified")??r.headers.get("date")??updatedAt;const json=await r.json()as{features?:unknown[]},parsed=parseRoadMetringPoints(json);all.push(...parsed);const count=json.features?.length??0;if(count<2000)break;offset+=count;}const unique=new Map(all.map(p=>[`${p.road}:${p.km}:${p.direction??""}:${p.lat.toFixed(5)}:${p.lng.toFixed(5)}`,p]));return{points:[...unique.values()],updatedAt};}
async function getStaticContext():Promise<StaticContext>{if(staticCache&&staticCache.expires>Date.now())return staticCache;const[siteFeed,metering,areaFeed]=await Promise.all([fetchText(URLS.sites,21600),fetchRoadPoints(),fetchText(URLS.rwsAreas,21600).catch(()=>({text:"{\"features\":[]}",updatedAt:null}))]);const raw=parseRawMeasurementSites(siteFeed.text),sites=mapMeasurementSites(raw,metering.points),rwsAreas=parseRwsRestAreas(areaFeed.text,metering.points);staticCache={expires:Date.now()+STATIC_TTL,sites,roadPoints:metering.points,rwsAreas,rawSiteCount:raw.length,siteUpdatedAt:siteFeed.updatedAt,meteringUpdatedAt:metering.updatedAt,areasUpdatedAt:areaFeed.updatedAt};return staticCache;}
const rushHour=()=>{const h=Number(new Intl.DateTimeFormat("nl-NL",{timeZone:"Europe/Amsterdam",hour:"2-digit",hour12:false}).format(new Date()));return(h>=6&&h<10)||(h>=15&&h<19);};
async function weatherMap(locations:CandidateLocation[]){if(!locations.length)return new Map<string,WeatherSnapshot>();const u=locations.filter((x,i,a)=>a.findIndex(y=>y.id===x.id)===i).slice(0,100),p=new URLSearchParams({latitude:u.map(x=>x.lat).join(","),longitude:u.map(x=>x.lng).join(","),current:"precipitation,wind_gusts_10m,visibility,weather_code",timezone:"Europe/Amsterdam"}),r=await fetch(`${URLS.weather}?${p}`,{next:{revalidate:300}});if(!r.ok)throw new Error(`Open-Meteo HTTP ${r.status}`);const raw=await r.json()as unknown,items=Array.isArray(raw)?raw:[raw],map=new Map<string,WeatherSnapshot>();u.forEach((l,i)=>{const c=(items[i]as{current?:Record<string,unknown>}|undefined)?.current??{};map.set(l.id,{precipitation:Number(c.precipitation??0),windGusts:Number(c.wind_gusts_10m??0),visibility:Number(c.visibility??0),weatherCode:c.weather_code===undefined?null:Number(c.weather_code),observedAt:typeof c.time==="string"?c.time:null});});return map;}

export async function GET(){
 const generatedAt=new Date().toISOString();let staticContext:StaticContext|null=null;try{staticContext=await getStaticContext();}catch{}
 const sites=staticContext?.sites??[],sources:SourceStatus[]=[
  {id:"ndw-sites",name:"NDW fysieke meetlocaties",ok:sites.length>0,updatedAt:staticContext?.siteUpdatedAt??null,error:sites.length?null:"Geen A-wegmeetpunten gekoppeld",lineage:"NDW AVG meetlocaties"},
  {id:"rws-metering",name:"RWS A-wegmetrering",ok:(staticContext?.roadPoints.length??0)>0,updatedAt:staticContext?.meteringUpdatedAt??null,error:(staticContext?.roadPoints.length??0)>0?null:"RWS metrering niet beschikbaar",lineage:"Rijkswaterstaat NWB metrering"},
  {id:"ndw-flow",name:"NDW snelheid + intensiteit",ok:false,updatedAt:null,error:null,lineage:"NDW/RWS fysieke detectoren"},
  {id:"rws-matrix",name:"RWS matrixsignalen",ok:false,updatedAt:null,error:null,lineage:"RWS wegkantsystemen via NDW"},
  {id:"ndw-current",name:"NDW Actueel Beeld",ok:false,updatedAt:null,error:null,lineage:"NDW situatiepublicatie"},
  {id:"rws-locations",name:"RWS verzorgingsplaatsen",ok:(staticContext?.rwsAreas.length??0)>0,updatedAt:staticContext?.areasUpdatedAt??null,error:(staticContext?.rwsAreas.length??0)>0?null:"Geen RWS locaties gekoppeld",lineage:"Rijkswaterstaat GeoData"},
  {id:"weather",name:"Open-Meteo weer",ok:false,updatedAt:null,error:null,lineage:"Open-Meteo"}
 ];
 let events=parseEvents(""),matrix=parseMatrix(""),samples=parseTrafficSamples("",new Map()),weather=new Map<string,WeatherSnapshot>();const siteMap=new Map(sites.map(x=>[x.id,x]));
 await Promise.all([
  fetchText(URLS.traffic,20).then(x=>{samples=parseTrafficSamples(x.text,siteMap);sources[2]={...sources[2],ok:samples.length>0,updatedAt:x.updatedAt,error:samples.length?null:"Geen verse detectorwaarden"};}).catch(e=>{sources[2].error=e instanceof Error?e.message:"Niet bereikbaar";}),
  fetchText(URLS.matrix,20).then(x=>{matrix=parseMatrix(x.text);sources[3]={...sources[3],ok:true,updatedAt:x.updatedAt};}).catch(e=>{sources[3].error=e instanceof Error?e.message:"Niet bereikbaar";}),
  fetchText(URLS.current,20).then(x=>{events=parseEvents(x.text);sources[4]={...sources[4],ok:true,updatedAt:x.updatedAt??(stripTags(tagValue(x.text,"publicationTime")??"")||null)};}).catch(e=>{sources[4].error=e instanceof Error?e.message:"Niet bereikbaar";})
 ]);
 const candidates=[...OPERATOR_LOCATIONS,...(staticContext?.rwsAreas??[])];try{weather=await weatherMap(candidates);sources[6]={...sources[6],ok:weather.size>0,updatedAt:[...weather.values()][0]?.observedAt??generatedAt};}catch(e){sources[6].error=e instanceof Error?e.message:"Niet bereikbaar";}
 const sampleMap=new Map(samples.map(x=>[x.siteId,x])),segments=buildSegments(sites),advice:StandbyAdvice[]=[];
 for(const seg of segments){const match=findStandby(seg,candidates);if(!match)continue;const sensor=sensorMetrics(seg,sampleMap),msi=segmentMatrix(matrix,seg),inc=segmentEvents(seg,events),w=weather.get(match.location.id)??null,wp=weatherScore(w),corroboratingSignals=[sensor.sensorCount>0&&sensor.congestionIndex>=25,msi.clusters>0,inc.items.length>0,wp>=2].filter(Boolean).length;let score=Math.round(sensor.score+msi.score+inc.score+wp+(corroboratingSignals>=2?4:0));if(!sensor.sensorCount)score=Math.min(score,48);if(corroboratingSignals<=1)score=Math.min(score,52);score=Math.max(0,Math.min(96,score));const highCoverage=sensor.sensorCount>=2&&sensor.directionCount>=2,confidence:StandbyAdvice["confidence"]=highCoverage&&corroboratingSignals>=2&&sources[2].ok&&sources[3].ok?"hoog":sensor.sensorCount>=1&&corroboratingSignals>=1?"middel":"laag",pressure:StandbyAdvice["pressure"]=score>=65?"hoog":score>=38?"verhoogd":"rustig",maxSnap=seg.sites.length?Math.max(...seg.sites.map(x=>x.mappingDistanceMeters)):0,reasons=[`${seg.road} km ${seg.kmFrom}–${seg.kmTo}: vast segment van exact 5 km`,sensor.sensorCount?`${sensor.sensorCount} verse fysieke detector(en), ${sensor.directionCount} rijrichtinggroep(en): ${sensor.averageSpeedKph??"—"} km/u mediaan${sensor.flowVehiclesPerHour!==null?`, ${sensor.flowVehiclesPerHour} vtg/u mediaan`:""}`:"geen verse fysieke snelheid/intensiteit — hoge score automatisch geblokkeerd",`meetpunten zijn aan officiële RWS A-wegmetrering gekoppeld (max. snapafstand ${maxSnap} m)`,msi.clusters?`${msi.clusters} matrixcluster(s) uitsluitend binnen dit hectometerbereik`:"geen actieve matrixmaatregelen binnen dit 5-km-deel",inc.accidents?`${inc.accidents} actueel ongeval(len) ruimtelijk bij dit wegdeel`:null,inc.obstructions?`${inc.obstructions} actuele blokkade(s)/obstakels ruimtelijk bij dit wegdeel`:null,corroboratingSignals>=2?`${corroboratingSignals} verschillende actuele signalen bevestigen de druk`:"onvoldoende bronbevestiging voor hoge zekerheid",match.location.knownOperationalLocation?"stand-byplek is operationeel bekend":"officiële RWS-verzorgingsplaats; operationele geschiktheid nog te valideren"].filter((x):x is string=>Boolean(x));advice.push({id:`${seg.road}-${seg.kmFrom}-${seg.kmTo}`,road:seg.road,segmentName:`${seg.road} km ${seg.kmFrom}–${seg.kmTo}`,kmFrom:seg.kmFrom,kmTo:seg.kmTo,score,pressure,confidence,recommendedUnits:score>=72&&confidence!=="laag"?2:score>=38?1:0,sensorCount:sensor.sensorCount,averageSpeedKph:sensor.averageSpeedKph,flowVehiclesPerHour:sensor.flowVehiclesPerHour,congestionIndex:sensor.congestionIndex,localEvents:inc.items.length,accidents:inc.accidents,obstructions:inc.obstructions,matrixClusters:msi.clusters,lowSpeedMatrixClusters:msi.lowSpeed,corroboratingSignals,reasons,weather:w,standby:{id:match.location.id,name:match.location.name,address:match.location.address,lat:match.location.lat,lng:match.location.lng,kind:match.location.kind,knownOperationalLocation:match.location.knownOperationalLocation}});}
 advice.sort((a,b)=>b.score-a.score||b.corroboratingSignals-a.corroboratingSignals||a.road.localeCompare(b.road,"nl",{numeric:true})||a.kmFrom-b.kmFrom);const byRoad=matrixSummary(matrix),activeSignals=byRoad.reduce((n,x)=>n+x.active,0);
 return NextResponse.json({generatedAt,refreshAfterSeconds:30,region:"Noord-Brabant + Gelderland",events,advice,sources,matrix:{activeSignals,byRoad},meta:{eventCount:events.length,accidentCount:events.filter(x=>x.kind==="accident").length,obstructionCount:events.filter(x=>x.kind==="obstruction").length,trafficCount:events.filter(x=>x.kind==="traffic").length,closureCount:events.filter(x=>x.kind==="closure").length,segmentCount:segments.length,measuredSiteCount:samples.length,rushHour:rushHour(),modelVersion:"0.4-rws-nwb-fusion-5km",note:`Vaste 5-km-segmenten. ${sites.length} NDW meetlocaties zijn via coördinaat gekoppeld aan officiële RWS A-wegmetrering. Snelheid/intensiteit is leidend; lokale matrixsignalen, incidenten en weer dienen als bevestiging. Ambigue meetpuntkoppelingen worden verworpen.`}},{headers:{"Cache-Control":"no-store, max-age=0"}});
}